# Dependencies
node_modules/
vendor/

# Environment files
.env
.env.*
!.env.example

# Build output
dist/
.output/
.nuxt/
*.tsbuildinfo

# Packaging (Tauri bundler)
.stage/
dist-app/
# Compiled JS emitted alongside TS sources (regenerated by the build)
src/frontend/src/**/*.js
src/shared/*.js

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Testing
coverage/
playwright-report/
test-results/

# Claude Code
.claude/

# KS Workflows
# Design/architecture notes (AGENTS.md sends docs here). Ignored so new notes
# stay local — but not "never tracked": some get force-added anyway, see
# `git ls-files project_docs`. It is a plain directory, not a symlink; a
# separate, long-diverged copy lives in ~/Dropbox/@Work/@project_docs/nexus.
project_docs
.worktrees/

# Claude Code local settings
settings.local.json
